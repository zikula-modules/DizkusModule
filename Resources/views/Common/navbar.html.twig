<nav class="navbar navbar-inverse dizkus-main" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-main-collapse">
            <span class="sr-only">{{ __('Toggle navigation') }}</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ path('zikuladizkusmodule_forum_index') }}">{{ __('Dizkus Forum') }}</a>
    </div>

<!-- Collect the nav links, forms, and other content for toggling -->
    <div id="navbar-main-collapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
            {% if currentUser.loggedIn == true and app.request.attributes.get('_route') == 'zikuladizkusmodule_forum_index' and settings.favorites_enabled %}
            {% if favorites is defined %}
                    <li><a class="fa fa-comments" href="{{ path('zikuladizkusmodule_user_showallforums') }}" title="{{ __("Show all forums") }}">&nbsp;{{ __("Show all forums") }}</a></li>
                {% else %}
                    <li><a class="fa fa-heart" href="{{ path('zikuladizkusmodule_user_showfavorites') }}" title="{{ __("Show favourite forums only") }}">&nbsp;{{ __("Show favourite forums only") }}</a></li>
                {% endif %}
            {% endif %}
            <li><a class="fa fa-comments" title="{{ __("View latest posts") }}" href="{{ path('zikuladizkusmodule_topic_viewlatest') }}">&nbsp;{{ __('View latest posts') }}</a></li>
            {% if currentUser.loggedIn == false %}
                {#{assign value="index.php?"|cat:$smarty.server.QUERY_STRING|urlencode var='redirect'} @TODO  #}
                <li><a title="{{ __("Log-in") }}" href="{{ path("zikulausersmodule_user_login") }}">{{ __("Log-in") }}</a></li>
                <li><a title="{{ __("Register") }}" href="{{ path("zikulausersmodule_user_register") }}">{{ __("Register") }}</a></li>
                <li><a class="tooltips" title="{{ __("Search forums") }}" href="{{ path("zikulasearchmodule_user_form") }}"><i class="fa fa-search"></i></a></li>
            {% else %}
                <li><a class="fa fa-comment" title="{{ __('View my posts') }}" href="{{ path('zikuladizkusmodule_user_mine') }}">&nbsp;{{ __('View my posts') }}</a></li>
                <li><a class="tooltips" title="{{ __('Search forums') }}" href="{{ path("zikulasearchmodule_user_form") }}"><i class="fa fa-search"></i></a></li>
                <li><a class="tooltips" title="{{ __('Personal settings') }}" href="{{ path("zikuladizkusmodule_user_prefs") }}"><i class="fa fa-user"></i><i class="fa fa-wrench"></i></a></li>
            {% endif %}
            {% if hasPermission('Dizkus::', '.*', 'ACCESS_ADMIN') %}
               <li><a class="tooltips" title="{{ __("Administrate Dizkus") }}" href="{{ path('zikuladizkusmodule_admin_index') }}"><i class="fa fa-cogs"></i></a></li>
            {% endif %}
        </ul>
    </div><!-- /.navbar-collapse -->       
</nav>